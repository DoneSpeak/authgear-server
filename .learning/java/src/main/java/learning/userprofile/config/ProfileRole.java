package learning.userprofile.config;

/**
 * Role used for attribute access control.
 * Mirrors Authgear: end_user | bearer | portal_ui.
 */
public enum ProfileRole {
    END_USER,
    BEARER,
    PORTAL_UI
}
