package learning.userprofile.config;

/**
 * Type of a custom attribute for validation.
 * Mirrors Authgear CustomAttributeType.
 */
public enum CustomAttributeType {
    STRING,
    NUMBER,
    INTEGER,
    ENUM,
    PHONE_NUMBER,
    EMAIL,
    URL,
    COUNTRY_CODE
}
